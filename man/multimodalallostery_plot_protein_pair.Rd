% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutation_classification.R
\name{multimodalallostery_plot_protein_pair}
\alias{multimodalallostery_plot_protein_pair}
\title{plot protein pair}
\usage{
multimodalallostery_plot_protein_pair(
  analysis_result,
  point_size = 2.5,
  alpha = 0.7,
  base_size = 15,
  xlim = c(-1.5, 3),
  ylim = c(-1.5, 3),
  color_map = c(`Not significant (FDR >= 0.05)` = "grey90", `Other (neutral in both)` =
    "grey90", `Both promoting` = "#FFB0A5", `Both disrupting` = "#F4270C",
    `Promoting in X / Disrupting in Y` = "#F4AD0C", `Disrupting in X / Promoting in Y` =
    "#F1DD10", `Allosteric only in X` = "#1B38A6", `Allosteric only in Y` = "#75C2F6"),
  legend_order = c("Both promoting", "Both disrupting",
    "Promoting in X / Disrupting in Y", "Disrupting in X / Promoting in Y",
    "Allosteric only in X", "Allosteric only in Y", "Other (neutral in both)",
    "Not significant (FDR >= 0.05)")
)
}
\arguments{
\item{analysis_result}{List returned by multimodalallostery_analyze_protein_pair().}

\item{point_size}{Plot setting point_size ; see Usage for the current default.}

\item{alpha}{Plot setting alpha ; see Usage for the current default.}

\item{base_size}{Plot setting base_size ; see Usage for the current default.}

\item{xlim}{Plot setting xlim ; see Usage for the current default.}

\item{ylim}{Plot setting ylim ; see Usage for the current default.}

\item{color_map}{Named vector mapping classifications to plot colours.}

\item{legend_order}{Character vector defining classification legend order.}
}
\value{
The plot object returned by this workflow.
}
\description{
Current implementation for plot protein pair.
}
\details{
Provenance: f5a; source operation: plot_protein_pair.
MoCHI inputs retain original column names (Pos_ref, id, mean_kcal/mol,
std_kcal/mol where used). Positions generally use Pos_ref + 1.
Thresholds and filtering follow the current source workflow.
}
